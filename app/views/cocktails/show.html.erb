<h1><%= @cocktail.name %></h1>


<ul>
<% @doses.each do |dose| %>
  <li><%= "#{dose.description} #{dose.ingredient.name} | "%><%= link_to "Delete", dose_path(dose), method: :delete, data: { confirm: "Are you sure you want to delete?" } %> </li>
<% end %>
</ul>

<br>

<%= render 'doses/form' %>
<br>
<%= link_to 'Back', cocktails_path %>

